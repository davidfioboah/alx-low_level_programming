#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings
 * @s1: string1
 * @s2: string2
 * Return: result(integer)
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
